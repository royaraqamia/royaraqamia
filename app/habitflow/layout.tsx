import { HabitFlowLayout } from '@/frontend/ui/habitflow/habitflow-layout';

export default function HabitFlowLayoutRoute({ children }: { children: React.ReactNode }) {
  return <HabitFlowLayout>{children}</HabitFlowLayout>;
}
